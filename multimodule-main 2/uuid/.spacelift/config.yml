version: 1
module_version: 0.3.1
    before_init:
        - echo "checking formatting"
        - terraform state list

tests:
  - name: No-op Test
    project_root: examples/default
    before_init:
        - echo "checking formatting"
        - terraform state list    

